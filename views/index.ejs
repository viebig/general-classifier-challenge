<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="utf-8">
    <title>Classifier</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body {
            background-color: #777;
            font-family: Arial, Helvetica, sans-serif;
            font-weight: bold;
            font-size: 25pt;
            color: #fff;
            text-align: center;
        }
        button {
            padding: 20px;
            border-radius: 5px;
            margin: 10px;
            display: inline;
        }
        .box {
            border: 2px solid #000;
            background-color: #fff;
            color: #000;
            padding: 20px;
            width: 70%;
            margin: auto;
        }
    </style>
    <script>
      document.addEventListener('keydown', function (event) {
        switch (event.keyCode) {
            case 37:
                if(document.getElementById("left")) {
                    document.getElementById("left").click();
                }
                break;
            case 39:
                if(document.getElementById("right")) {
                    document.getElementById("right").click();
                }
                break;
            case 32:
                if (document.getElementById("skip")) {
                    document.getElementById("skip").click();
                }
                break;
            case 49:
                if (document.getElementById("category0")) {
                    document.getElementById("category0").click();
                }
                break;
            case 50:
                if (document.getElementById("category1")) {
                    document.getElementById("category1").click();
                }
            break;
            case 51:
                if (document.getElementById("category2")) {
                    document.getElementById("category2").click();
                }
                break;
        
            default:
                break;
        }
        }, true);
    </script>
</head>

<body>
    <h1>GENERAL CLASSIFIER</h1>
    <p>o melhor classificador que você verá hoje</p>
    <form action="/" method="POST">
        <label for="phrase"></label>
        <div class="box">
            <%= number %> <%= prhase %>
        </div>
        <%  if (keyPlace != 'next' && keyPlace != 'middle') { %>
                <button name="action" value="-1" id='left' >previous</button>
            <% }
            if (keyPlace != 'previous' && keyPlace != 'middle') { %>
                <button name="action" value="1" id='right'>next</button>
            <br>
            <%if(!phraseClassified) {
                for(var i=0; i < Category.length; i++) {%>
                    <button name="category" value="<%= i %>" id='category<%= i %>'>
                        <%= Category[i] %>
                    </button>
                <% }
                if(!skipFail){%>
                    <button name="skip" value="true" id='skip'>skip</button>
                <% }
            }            
        } %>
        <% if(downloadFile) { %>
            <a href="/download" target="_blank" download>
                Download the file
            </a>
        <% }  %>
    </form>
</body>

</html>