<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="utf-8">
    <title>Classifier</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script type="text/javascript" src="hotkeys.js">

    </script>
    <style>
        body {
            background-color: #777;
            font-family: Arial, Helvetica, sans-serif;
            font-weight: bold;
            font-size: 25pt;
            color:#fff;
            text-align: center;
        }
        button{
            padding: 20px;
            border-radius: 5px;
            margin: 10px;
            display: inline;
        }
        .box{
            border: 2px solid #000;
            background-color: #fff;
            color: #000;
            padding: 20px;
            width: 70%;
            margin: auto;
        }
    </style>
</head>
<body>
    <h1>GENERAL CLASSIFIER</h1>
    <p>o melhor classificador que você verá hoje</p>
    <form action="/" method="POST">
        <label for="phrase"></label>
        <div class="box">
            <%= prhase %>
        </div>
    <%  if (firstOrLast != 'first' && firstOrLast != 'middlePhrase') { %>
            <button name="action" value="-1">previous</button>
    <% }
         if (firstOrLast != 'last' && firstOrLast != 'middlePhrase' ) { %>
             <button name="action" value="1">next</button>
    <% } %>
            <br>
            <% for(var i=0; i < Category.length; i++) {%>
                    <button name="category" value="<%= i %>"><%= Category[i] %></button>
                <% } %>
            <% if(!SkipFail){%>
                <button name="skip" value="true">skip</button>
            <% } %>
        </form>
</body>
</html>